import { GenderIconPipe } from './gender-icon.pipe';

describe('GenderIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderIconPipe();
    expect(pipe).toBeTruthy();
  });
});
